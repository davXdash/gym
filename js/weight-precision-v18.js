const SNAP='gym-snapshot-v11';
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}};
function applyWeightPrecision(){const rows=read(SNAP,{weights:[]}).weights||[],values=rows.map(x=>Number(x.weight_kg)).filter(Number.isFinite);const last=values.at(-1),latest=document.querySelector('#latest-weight');if(latest&&Number.isFinite(last))latest.textContent=`${last.toLocaleString('de-DE',{minimumFractionDigits:2,maximumFractionDigits:2})} kg`;if(!values.length)return;const max=Math.max(...values).toLocaleString('de-DE',{minimumFractionDigits:2,maximumFractionDigits:2}),min=Math.min(...values).toLocaleString('de-DE',{minimumFractionDigits:2,maximumFractionDigits:2});['#weight-chart','#weight-chart-2'].forEach(sel=>{const texts=document.querySelectorAll(`${sel} svg text`);if(texts[0])texts[0].textContent=max;if(texts[1])texts[1].textContent=min})}
window.addEventListener('load',()=>{setTimeout(applyWeightPrecision,350);setTimeout(applyWeightPrecision,1000)});document.addEventListener('click',e=>{if(e.target.closest('#save-weight'))setTimeout(applyWeightPrecision,800)});