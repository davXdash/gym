import {createClient} from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
import {SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY} from './supabase-config.js';

const supabase=createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY);
const SNAP='gym-snapshot-v11',QUEUE='gym-queue-v11',OFF='gym-offline-v11';
const q=(s,r=document)=>r.querySelector(s);
const qa=(s,r=document)=>[...r.querySelectorAll(s)];
const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}};
const write=(k,v)=>localStorage.setItem(k,JSON.stringify(v));
const num=v=>{const n=Number(String(v??'').replace(',','.'));return Number.isFinite(n)?n:null};
const online=()=>navigator.onLine&&localStorage.getItem(OFF)!=='1';
const day=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'Europe/Berlin'}).format(new Date());
const nowClock=()=>new Intl.DateTimeFormat('de-DE',{timeZone:'Europe/Berlin',hour:'2-digit',minute:'2-digit',hour12:false}).format(new Date());
const kg=v=>num(v)==null?'–':num(v).toLocaleString('de-DE',{minimumFractionDigits:2,maximumFractionDigits:2});
let saving=false;

function berlinParts(value){const d=new Date(value);if(Number.isNaN(d.getTime()))return{date:String(value||'').slice(0,10),time:String(value||'').slice(11,16)};const parts=new Intl.DateTimeFormat('sv-SE',{timeZone:'Europe/Berlin',year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit',hour12:false}).formatToParts(d),p=Object.fromEntries(parts.map(x=>[x.type,x.value]));return{date:`${p.year}-${p.month}-${p.day}`,time:`${p.hour}:${p.minute}`}}
function berlinMinute(value){const p=berlinParts(value);return`${p.date}T${p.time}`}
function stamp(value){const d=new Date(value);return Number.isNaN(d.getTime())?String(value||''):new Intl.DateTimeFormat('de-DE',{timeZone:'Europe/Berlin',day:'2-digit',month:'2-digit',year:'2-digit',hour:'2-digit',minute:'2-digit'}).format(d).replace(',',' ·')}
function rows(){return[...(read(SNAP,{weights:[]}).weights||[])].filter(x=>num(x.weight_kg)!=null).sort((a,b)=>new Date(a.measured_at)-new Date(b.measured_at))}
function setCurrentTime(force=false){const el=q('#weight-time');if(el&&(force||!el.value))el.value=nowClock()}
function setSnapshotWeights(data){const s=read(SNAP,{weights:[]});s.weights=data||[];write(SNAP,s)}

function bindIndependentChips(){for(const name of['toilet','food','late','trained']){const group=q(`[data-chip-group="${name}"]`);if(!group)continue;qa('.chip',group).forEach(button=>{if(button.dataset.weightBound==='1')return;button.dataset.weightBound='1';button.addEventListener('click',e=>{e.preventDefault();qa('.chip',group).forEach(x=>x.classList.remove('active'));button.classList.add('active')})})}}

function summary(data){const last=data.at(-1),prev=data.at(-2),values=data.map(x=>num(x.weight_kg)),change=last&&prev?num(last.weight_kg)-num(prev.weight_kg):null,min=values.length?Math.min(...values):null,max=values.length?Math.max(...values):null;return`<div class="weight-summary"><article><small>AKTUELL</small><strong>${last?kg(last.weight_kg):'–'} kg</strong><span>${last?stamp(last.measured_at):'Noch kein Wert'}</span></article><article><small>LETZTE ÄNDERUNG</small><strong>${change==null?'–':`${change>0?'+':''}${kg(change)} kg`}</strong><span>gegenüber vorher</span></article><article><small>SPANNE</small><strong>${min==null?'–':`${kg(min)}–${kg(max)}`}</strong><span>kg · sichtbarer Zeitraum</span></article></div>`}
function coordinateChart(data){if(!data.length)return'<div class="weight-empty">Noch keine Gewichtsdaten.</div>';const shown=data.slice(-20),values=shown.map(x=>num(x.weight_kg)),lo=Math.min(...values),hi=Math.max(...values),padding=Math.max(.20,(hi-lo)*.25),min=Math.floor((lo-padding)*10)/10,max=Math.ceil((hi+padding)*10)/10,span=Math.max(.4,max-min),W=440,H=300,L=66,R=18,T=28,B=62,PW=W-L-R,PH=H-T-B,x=i=>L+i*PW/Math.max(1,shown.length-1),y=v=>T+(max-v)/span*PH;let grid='';for(let i=0;i<6;i++){const yy=T+i*PH/5,v=max-i*span/5;grid+=`<line x1="${L}" y1="${yy}" x2="${W-R}" y2="${yy}" class="wg-grid"/><text x="${L-10}" y="${yy+4}" text-anchor="end" class="wg-y">${kg(v)}</text>`}const labelIndexes=new Set([0,shown.length-1]);if(shown.length>2)labelIndexes.add(Math.floor((shown.length-1)/2));if(shown.length>6){labelIndexes.add(Math.floor((shown.length-1)/3));labelIndexes.add(Math.floor((shown.length-1)*2/3))}const dots=shown.map((r,i)=>`<g class="wg-point"><circle cx="${x(i)}" cy="${y(num(r.weight_kg))}" r="5"/><text x="${x(i)}" y="${Math.max(16,y(num(r.weight_kg))-11)}" text-anchor="middle">${kg(r.weight_kg)}</text></g>`).join(''),xlabels=shown.map((r,i)=>labelIndexes.has(i)?`<text x="${x(i)}" y="${H-30}" text-anchor="middle" class="wg-x">${new Intl.DateTimeFormat('de-DE',{timeZone:'Europe/Berlin',day:'2-digit',month:'2-digit'}).format(new Date(r.measured_at))}</text>`:'').join(''),segments=shown.slice(1).map((r,i)=>`<line x1="${x(i)}" y1="${y(num(shown[i].weight_kg))}" x2="${x(i+1)}" y2="${y(num(r.weight_kg))}" class="wg-segment"/>`).join('');return`<div class="weight-plot"><svg viewBox="0 0 ${W} ${H}" role="img" aria-label="Gewicht nach Messdatum"><text x="12" y="18" class="wg-unit">kg</text>${grid}<line x1="${L}" y1="${T}" x2="${L}" y2="${H-B}" class="wg-axis"/><line x1="${L}" y1="${H-B}" x2="${W-R}" y2="${H-B}" class="wg-axis"/>${segments}${dots}${xlabels}<text x="${(L+W-R)/2}" y="${H-5}" text-anchor="middle" class="wg-title">Messdatum</text></svg></div>`}
function historyList(data){const shown=[...data].reverse().slice(0,8);return`<div class="weight-history"><div class="weight-history-head"><strong>Letzte Messungen</strong><span>${data.length} Werte</span></div>${shown.map((r,i)=>{const prev=shown[i+1],delta=prev?num(r.weight_kg)-num(prev.weight_kg):null;return`<div class="weight-history-row"><span>${stamp(r.measured_at)}</span><strong>${kg(r.weight_kg)} kg</strong><small>${delta==null?'':`${delta>0?'+':''}${kg(delta)} kg`}</small></div>`}).join('')}</div>`}
function paint(){const data=rows(),last=data.at(-1),dash=q('#latest-weight');if(dash)dash.textContent=last?`${kg(last.weight_kg)} kg`:'Noch kein Gewicht';const main=q('#weight-chart');if(main)main.innerHTML=`<div class="weight-dashboard">${summary(data)}${coordinateChart(data)}${historyList(data)}</div>`;const progress=q('#weight-chart-2');if(progress)progress.innerHTML=`<div class="weight-dashboard compact">${summary(data)}${coordinateChart(data)}</div>`}

async function refreshWeights(){if(!online()){paint();return rows()}const {data:{session}}=await supabase.auth.getSession();if(!session)return[];const r=await supabase.from('weigh_ins').select('*').eq('user_id',session.user.id).order('measured_at');if(r.error)throw r.error;setSnapshotWeights(r.data||[]);paint();return r.data||[]}
async function duplicateAtMinute(minute,excludeId=null){const data=online()?await refreshWeights():rows();return data.some(r=>String(r.id)!==String(excludeId||'')&&berlinMinute(r.measured_at)===minute)}

async function insertWeight(payload){if(online()){const r=await supabase.from('weigh_ins').insert(payload);if(r.error)throw r.error;await refreshWeights()}else{const s=read(SNAP,{weights:[]});s.weights=[...(s.weights||[]),payload];write(SNAP,s);const queue=read(QUEUE,[]);queue.push({type:'weight',payload,queued_at:new Date().toISOString()});write(QUEUE,queue);paint()}}
async function saveWeight(e){const button=e.target.closest('#save-weight');if(!button)return;e.preventDefault();if(saving)return;const status=q('#weight-status'),input=q('#weight-input'),time=q('#weight-time'),v=num(input?.value);if(!(v>=30&&v<=300)){if(status)status.textContent='Bitte gültiges Gewicht eintragen.';return}const clock=time?.value||nowClock(),minute=`${day()}T${clock}`;saving=true;button.disabled=true;try{if(await duplicateAtMinute(minute)){if(status)status.textContent=`Für ${clock} Uhr ist bereits ein Gewicht gespeichert.`;return}const localDate=new Date(`${day()}T${clock}:00`),measured_at=localDate.toISOString(),val=n=>q(`[data-chip-group="${n}"] .active`)?.dataset.value||'unknown',trained=val('trained'),{data:{session}}=await supabase.auth.getSession();if(!session)throw new Error('Bitte neu anmelden.');await insertWeight({user_id:session.user.id,weight_kg:v,measured_at,toilet_status:val('toilet'),food_status:val('food'),late_meal:val('late'),trained_previous_day:trained==='yesterday',notes:trained==='today'?'training_today':''});if(status)status.textContent='Gespeichert.';setCurrentTime(true)}catch(err){if(status)status.textContent=err?.message||String(err)}finally{saving=false;button.disabled=false}}

const labelMap={toilet_status:{unknown:'Keine Angabe',before:'Nein',after:'Ja'},food_status:{unknown:'Keine Angabe',fasted:'Ja',ate:'Nein'},late_meal:{unknown:'Keine Angabe',none:'Nein',small:'Klein',normal:'Normal',large:'Groß'}};
function trainingValue(r){return r.notes==='training_today'?'today':r.trained_previous_day?'yesterday':'none'}
function option(value,label,current){return`<option value="${value}" ${value===current?'selected':''}>${label}</option>`}
function selectOptions(map,current){return Object.entries(map).map(([v,l])=>option(v,l,current)).join('')}
function ensureManager(){let d=q('#weight-manager');if(d)return d;d=document.createElement('dialog');d.id='weight-manager';d.className='weight-manager';document.body.append(d);return d}
function managerButton(){const page=q('#page-weight');if(!page||q('#weight-manage-button'))return;const head=q('#weight-chart')?.closest('.chart-card')?.querySelector('h3')||q('#page-weight .page-head h2');if(!head)return;const b=document.createElement('button');b.id='weight-manage-button';b.type='button';b.className='secondary';b.textContent='Messungen verwalten';head.insertAdjacentElement('afterend',b);b.onclick=openManager}
async function openManager(){const d=ensureManager();d.innerHTML='<div class="weight-manager-card"><header><div><small>GEWICHT</small><h2>Messungen verwalten</h2></div><button type="button" data-manager-close>Schließen</button></header><button type="button" class="primary weight-backfill" data-backfill>+ Alten Wert nachtragen</button><p data-manager-status>Datenbank wird geladen …</p><div data-manager-list></div></div>';q('[data-manager-close]',d).onclick=()=>d.close();q('[data-backfill]',d).onclick=openBackfill;d.showModal();try{const data=await refreshWeights();renderManagerList(d,data)}catch(e){q('[data-manager-status]',d).textContent=e.message}}
function renderManagerList(d,data){const status=q('[data-manager-status]',d),list=q('[data-manager-list]',d);status.textContent=`${data.length} gespeicherte Messungen`;list.innerHTML=[...data].reverse().map(r=>`<article class="weight-manager-row"><div><strong>${kg(r.weight_kg)} kg</strong><small>${stamp(r.measured_at)}</small></div><button type="button" data-edit-id="${r.id}">Bearbeiten</button><button type="button" class="danger" data-delete-id="${r.id}">Löschen</button></article>`).join('')||'<p>Noch keine Messungen.</p>';qa('[data-edit-id]',list).forEach(b=>b.onclick=()=>editMeasurement(data.find(r=>String(r.id)===b.dataset.editId),d));qa('[data-delete-id]',list).forEach(b=>b.onclick=()=>deleteMeasurement(data.find(r=>String(r.id)===b.dataset.deleteId),d))}
function formFields(values={}){return`<div class="weight-edit-grid"><label>Gewicht in kg<input name="weight" inputmode="decimal" value="${values.weight??''}" required></label><label>Datum<input name="date" type="date" value="${values.date??day()}" required></label><label>Messzeit<input name="time" type="time" value="${values.time??nowClock()}" required></label><label>Toilette erledigt?<select name="toilet">${selectOptions(labelMap.toilet_status,values.toilet||'unknown')}</select></label><label>Nüchtern?<select name="food">${selectOptions(labelMap.food_status,values.food||'unknown')}</select></label><label>Späte Mahlzeit?<select name="late">${selectOptions(labelMap.late_meal,values.late||'unknown')}</select></label><label>Training<select name="trained">${option('none','Keine Angabe',values.trained||'none')}${option('yesterday','Gestern',values.trained||'none')}${option('today','Heute',values.trained||'none')}</select></label></div>`}
async function openBackfill(){const d=ensureManager();d.innerHTML=`<form class="weight-manager-card" data-backfill-form><header><div><small>HISTORISCHER WERT</small><h2>Gewicht nachtragen</h2></div><button type="button" data-backfill-cancel>Abbrechen</button></header><p class="weight-manager-hint">Gewicht, Datum und Messzeit sind erforderlich. Die vier Zusatzangaben kannst du bei alten Werten auf „Keine Angabe“ lassen.</p>${formFields()}<p data-backfill-status></p><button class="primary" type="submit">Wert nachtragen</button></form>`;q('[data-backfill-cancel]',d).onclick=openManager;q('[data-backfill-form]',d).onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),weight=num(f.get('weight')),date=f.get('date'),time=f.get('time'),status=q('[data-backfill-status]',d);if(!(weight>=30&&weight<=300)||!date||!time){status.textContent='Bitte Gewicht, Datum und Messzeit prüfen.';return}const minute=`${date}T${time}`;try{if(await duplicateAtMinute(minute)){status.textContent='Für diesen Messzeitpunkt existiert bereits ein Eintrag.';return}const {data:{session}}=await supabase.auth.getSession();if(!session)throw new Error('Bitte neu anmelden.');const trained=f.get('trained');await insertWeight({user_id:session.user.id,weight_kg:weight,measured_at:new Date(`${date}T${time}:00`).toISOString(),toilet_status:f.get('toilet'),food_status:f.get('food'),late_meal:f.get('late'),trained_previous_day:trained==='yesterday',notes:trained==='today'?'training_today':''});await openManager()}catch(err){status.textContent=err.message}}}
async function editMeasurement(r){if(!r)return;const p=berlinParts(r.measured_at),d=ensureManager();d.innerHTML=`<form class="weight-manager-card" data-edit-form><header><div><small>MESSUNG BEARBEITEN</small><h2>${kg(r.weight_kg)} kg</h2></div><button type="button" data-edit-cancel>Abbrechen</button></header>${formFields({weight:String(r.weight_kg).replace('.',','),date:p.date,time:p.time,toilet:r.toilet_status||'unknown',food:r.food_status||'unknown',late:r.late_meal||'unknown',trained:trainingValue(r)})}<p data-edit-status></p><button class="primary" type="submit">Änderungen speichern</button></form>`;q('[data-edit-cancel]',d).onclick=openManager;q('[data-edit-form]',d).onsubmit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),weight=num(f.get('weight')),date=f.get('date'),time=f.get('time'),status=q('[data-edit-status]',d);if(!(weight>=30&&weight<=300)||!date||!time){status.textContent='Bitte Gewicht, Datum und Zeit prüfen.';return}const minute=`${date}T${time}`;try{if(await duplicateAtMinute(minute,r.id)){status.textContent='Für diesen Messzeitpunkt existiert bereits ein anderer Eintrag.';return}const trained=f.get('trained'),oldNotes=String(r.notes||''),notes=trained==='today'?'training_today':oldNotes==='training_today'?'':oldNotes,payload={weight_kg:weight,measured_at:new Date(`${date}T${time}:00`).toISOString(),toilet_status:f.get('toilet'),food_status:f.get('food'),late_meal:f.get('late'),trained_previous_day:trained==='yesterday',notes};const res=await supabase.from('weigh_ins').update(payload).eq('id',r.id);if(res.error)throw res.error;await refreshWeights();await openManager()}catch(err){status.textContent=err.message}}}
async function deleteMeasurement(r,manager){if(!r||!confirm(`${kg(r.weight_kg)} kg vom ${stamp(r.measured_at)} wirklich löschen?`))return;const status=q('[data-manager-status]',manager);try{const res=await supabase.from('weigh_ins').delete().eq('id',r.id);if(res.error)throw res.error;const data=await refreshWeights();renderManagerList(manager,data)}catch(e){status.textContent=e.message}}

function style(){if(q('#weight-style'))return;const s=document.createElement('style');s.id='weight-style';s.textContent=`#weight-chart,#weight-chart-2{min-height:0!important;overflow:visible!important}.weight-dashboard{display:grid;gap:14px}.weight-summary{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.weight-summary article{padding:11px;border:1px solid var(--line,#dce2dc);border-radius:15px;background:var(--surface-2,#f6f8f5)}.weight-summary small,.weight-summary span{display:block;color:var(--muted,#707970);font-size:.62rem}.weight-summary strong{display:block;margin:5px 0 3px;font-size:.92rem}.weight-plot{padding:8px 2px 0;border:1px solid var(--line,#dce2dc);border-radius:17px;background:var(--surface,#fff);overflow:hidden}.weight-plot svg{display:block;width:100%;height:auto;min-height:260px}.wg-grid{stroke:var(--line,#e0e5e0);stroke-width:1}.wg-axis{stroke:#667269;stroke-width:1.5}.wg-segment{stroke:#789381;stroke-width:2.2}.wg-point circle{fill:#356b4d;stroke:var(--surface,#fff);stroke-width:2.5}.wg-point text{fill:var(--text,#151a16);font-size:10px;font-weight:800}.wg-y,.wg-x,.wg-unit,.wg-title{fill:var(--muted,#707970);font-size:10px}.weight-history{border:1px solid var(--line,#dce2dc);border-radius:17px;overflow:hidden}.weight-history-head,.weight-history-row{display:grid;grid-template-columns:1fr auto auto;gap:10px;align-items:center;padding:10px 12px}.weight-history-head{background:var(--surface-2,#f6f8f5)}.weight-history-head span,.weight-history-row span,.weight-history-row small{color:var(--muted,#707970);font-size:.7rem}.weight-history-row{border-top:1px solid var(--line,#e0e5e0)}.weight-history-row strong{font-size:.8rem}.weight-empty{display:grid;place-items:center;min-height:180px;color:var(--muted,#707970)}#weight-manage-button{margin:8px 0 12px}.weight-manager{width:min(94vw,620px);max-height:88dvh;border:0;border-radius:24px;padding:0;background:var(--surface,#fff);color:var(--text,#171b18)}.weight-manager::backdrop{background:rgba(18,24,20,.48);backdrop-filter:blur(4px)}.weight-manager-card{padding:18px}.weight-manager-card header{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:14px}.weight-manager-card header h2{margin:2px 0}.weight-manager-card header button{border:1px solid var(--line);border-radius:12px;background:var(--surface-2);padding:9px 11px;color:inherit}.weight-manager-hint{color:var(--muted);font-size:.78rem;line-height:1.4}.weight-backfill{width:100%;margin-bottom:12px}.weight-manager-row{display:grid;grid-template-columns:1fr auto auto;gap:7px;align-items:center;padding:11px 0;border-top:1px solid var(--line)}.weight-manager-row small{display:block;color:var(--muted);margin-top:3px}.weight-manager-row button{min-height:38px;border:1px solid var(--line);border-radius:11px;background:var(--surface-2);color:inherit;padding:0 9px}.weight-manager-row button.danger{color:#9b3d34}.weight-edit-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.weight-edit-grid label{display:grid;gap:5px;color:var(--muted);font-size:.75rem}.weight-edit-grid input,.weight-edit-grid select{width:100%;min-height:46px;border:1px solid var(--line);border-radius:12px;background:var(--surface-2);color:inherit;padding:0 10px;font-size:16px}.weight-manager-card>.primary{width:100%;min-height:48px;margin-top:14px}@media(max-width:430px){.weight-summary{grid-template-columns:1fr}.weight-summary article{display:grid;grid-template-columns:1fr auto;align-items:center}.weight-summary article small,.weight-summary article span{grid-column:1}.weight-summary article strong{grid-column:2;grid-row:1/3;margin:0}.weight-history-head,.weight-history-row{grid-template-columns:1fr auto}.weight-history-row small{grid-column:2}.weight-plot svg{min-height:240px}.weight-edit-grid{grid-template-columns:1fr}.weight-manager-row{grid-template-columns:1fr auto}.weight-manager-row .danger{grid-column:2}}`;document.head.append(s)}
function install(){style();setCurrentTime();bindIndependentChips();paint();managerButton();document.addEventListener('click',saveWeight);document.addEventListener('click',e=>{if(e.target.closest('[data-page="weight"],[data-page="progress"],[data-page-link="weight"]'))setTimeout(()=>{setCurrentTime(true);bindIndependentChips();paint();managerButton()},40)});window.addEventListener('pageshow',()=>setTimeout(()=>{setCurrentTime();bindIndependentChips();paint();managerButton()},60))}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
